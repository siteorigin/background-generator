export const downloadNotification = (backgroundUrl) => `
    <div class="flex text-sm">
        <img src="${backgroundUrl}" class="w-20 h-20 mr-3" alt="">
        Your pattern is downloading! The Download should start shortly.
    </div>
`
